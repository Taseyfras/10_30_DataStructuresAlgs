var expect = require('chai').expect;
var unique = require('./unique.js');
var evens = [];

describe ('unique', function(){
	it('takes an array as a parameter', function (){
		expect(evens).to.be.a('array');
		expect(array[i] != array[j]);
			//});
		//});
	});
});